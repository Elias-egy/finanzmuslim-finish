# Gegenprüfung der P3-Spec, 26.09.2026

Lesender Review durch einen Claude-Plan-Agenten (Codex fiel mit 401 aus). Nichts geändert, Rechnungen mit Python auf
den erzeugten Daten nachgestellt. Wortgleich übernommen, Entscheidungen dazu stehen in `P3-SPEC.md` Abschnitt 10.

## BEFUND

1. **`nicht_gefunden`.** Die Spec sagt in `docs/rangfolge/P3-SPEC.md:68` „zählt als geprüft mit 0“. Der Code macht das Gegenteil:
   - `anlagen_regeln.py:15,176` legt `nicht_gefunden` unter „offen“ ab. Daraus wird „mind. x von N“ (Z. 181) ohne Punkte (Z. 220).
   - `test_anlagen_regeln.py:89-94` testet genau dieses Verhalten.
   - Nur der veraltete Docstring `anlagen_verdichten.py:10` sagt „gilt als geprüft“.
   - `recherche/anlagen.json` enthält 82 solche Einträge in 10 Häusern (etoro 16, fidelity 14, justtrade 13 …, scalable-prime 4). Alle diese Häuser blieben nichtBewertet.
   - Ohne Entscheidung widerspricht sich die Pflichtregel in Z. 68 selbst.
2. **Unklarer Ausgabeaufschlag.** Die Spec (Z. 65-67) verortet die Regel in `anlagen_verdichten.py`. Gerechnet wird aber in `anlagen_regeln.py:185-191`:
   - `AUFSCHLAG_FAKTOR` (Z. 45) kennt kein „unklar“. Eine fehlende Angabe ergibt ebenfalls None.
   - `test_anlagen_regeln.py:134-138` verlangt `punkte` = None.
   - `halalAnlagenPunkte` schreibt erst `bauen.py:552-553,584`, und nur wenn `punkte` im Beleg steht. „Immer“ stimmt deshalb nicht.
   - Die alte Regel steht noch in `src/data/vergleichHelfer.ts:55-59` und `src/lib/bewertung.ts:22-25,133-137`, getestet in `src/lib/bewertung.test.ts:92-99`.
   - Heute betroffen: `finanzen-net-zero-depot` halalEtfsFonds und `ing-direkt-depot` halalEtfsFonds und halalSukuk.
3. **Schlüssel fehlt statt null.** `bewertung.ts:139` nimmt bei fehlendem `halalAnlagenPunkte`-Schlüssel stillschweigend die Rohzahl x. Ein Fonds mit Aufschlag zählt dann 1,0. Die Spec regelt diesen Fall nicht.
4. **Nenner.** Die Spec teilt fest durch 22 (Z. 64). `bewertung.ts:140` teilt durch die Summe der N aus den Texten. Die Tests nutzen „8 von 8“, also N = 23 (`bewertung.test.ts:42,51,60,71`, `vergleichAssistent.test.ts:75,79-82`). Nur für echte Daten erzwingt `src/data/vergleiche.test.ts:81-93` die Werte 12/3/7.
5. **Signatur.** Die Schnittstelle in Z. 24 hat keinen Parameter für die Höchstpunkte. Die Kosten für Depot, Giro und Krypto brauchen aber `*_FINANZ_MAX` (vgl. `bewertung.ts:112`, `vergleichAssistent.ts:260`). Die Tests arbeiten mit Kunst-Maxima (`vergleichAssistent.test.ts:10`).
6. **Typ `Kategorie`.** Die Spec definiert 6 Werte (Z. 18). `bewertung.ts:31` exportiert einen gleichnamigen Typ mit 3 Werten, genutzt in `VergleichsSeite.tsx:26,83`, `src/data/vergleichAssistent.ts:3,100`, `vergleiche.test.ts:9`. Screener und Steuer laufen heute mit `kategorie: null` (`data/vergleichAssistent.ts:557,613`). Dafür hat `werteAus` einen eigenen Pfad (`lib/vergleichAssistent.ts:258,285,330`). Edelmetall hat keinen Baustein, Test 4 (Z. 125) greift dort nicht.
7. **„Kosten wie heute“ ist doppeldeutig.** `bewertung.ts:158-160` summiert alle Werte, auch ein positives `abzug` und Fremdschlüssel. `finanzNote` (`vergleichAssistent.ts:157-174`) nimmt nur die Schlüssel aus den Höchstpunkten plus `min(0, abzug)`. `werteAus` ohne Priorität nimmt die Finanzfluss-Rangnote (Z. 300).
8. **Ampel `teils` = 0,5 (Z. 58).** `bewertung.ts:104-105` wertet „teils“ als fehlend. Real betroffen ist Haspa mit keinDispoAbStart „teils“ (`vergleichKorrekturenDaten.ts:113`). Den Wert „unbekannt“ (`vergleichTypen.ts:14`) erwähnt die Spec nicht.
9. **Seite und geführter Vergleich laufen auseinander.** §6 verlangt dieselbe Nummer 1. `werteAus` wirft Zins-Tor „teils“ und rote Grundlagen (`BASIS`) raus (`vergleichAssistent.ts:177-181,286-290`, Test `vergleichAssistent.test.ts:106-118`). `rangfolge` rankt „teils“ mit halbem Halal-Teil und zählt zinsfreiesModell „schlecht“ nur als Abzug (Z. 59, 78). Heute passt Platz 1 zufällig: Krypto Bitvavo 4,72, Giro Consorsbank 4,90, Depot Scalable Free 4,87 (Partner). Aber DKB Girokonto (Dispo „schlecht“) und Wise („teils“) stehen gerankt und fehlen im geführten Vergleich. `empfehlbar` hängt an `raus === 0` (`data/vergleichAssistent.ts:662-674`).
10. **„Priorität nie Halal“ (Z. 116) wird heute verletzt.** `DEPOT_AUSWAHL` setzt halalAnteil 0,65 (`data/vergleichAssistent.ts:213-219`, wirkt in `lib/vergleichAssistent.ts:267,304`). `nebenSort` und `grundSort` sortieren nach Halal-Zählern, auch „mind.“ (`data/vergleichAssistent.ts:246-250,338-350,571-575`, addiert in `lib/vergleichAssistent.ts:250-254,318`). `grundSort` hängt auch ohne Antworten mit (`data/vergleichAssistent.ts:647-653`). Die Divisoren 11 und 23 sind veraltet, heute gelten 10 und 22.
11. **Steuer, Feldtypen.** `belegabruf` ist Ja/Nein als boolean (`steuersoftwareVergleich.ts:60`, Werte true oder null), die Spec (Z. 84) kennt nur Texte. „teils“ und „eingeschränkt“ kommen in den Daten nicht vor. Tatsächlich stehen dort „ja, EÜR nur in der Fassung Deluxe“ (Z. 191), „nur in der Fassung für Selbstständige, 94,95 €“ (Z. 346), „nur in der Fassung plus, 45,95 €“ (Z. 347). Ohne Regel landen QuickSteuer und SteuerSparErklärung mit dem falschen Grund „noch nicht geprüft“ in nichtBewertet.
12. **Steuer, Preis.** „Erster Eurobetrag“ widerspricht „Preis für eine Erklärung“ (Z. 85): bei WISO (Z. 417) und Taxfix (Z. 520) greift der Abo-Preis, bei STEUEReasy der Aktionspreis (Z. 170), bei SteuerSparErklärung die Grundfassung, obwohl Vermietung nur in „plus“ steckt.
13. **Steuer, Reihenfolge.** ELSTER und CHECK24 rechnen beide 5,0/5,0/5,0. Die Namensregel setzt CHECK24 nach vorn. Das bricht den Test `vergleichAssistent.test.ts:364-369` (erwartet ELSTER und `note` null), den Satz in `data/vergleichAssistent.ts:611` und den Kommentar in `steuersoftwareVergleich.ts:99-103`.
14. **Screener.** `etfs`, `depot` und `zakat` sind boolean und bei allen vier Apps true (`screenerVergleich.ts:95-99,156-160,219-223,267-271`), sie trennen nichts. `kostenlosNutzbar` laut Z. 92 zählt Islamicly („eine Prüfung als Bericht, danach Abo“, Z. 157) als 1, `kostenlosReicht` (`data/vergleichAssistent.ts:156-159`, Test `vergleichAssistent.test.ts:385-389`) zählt 0. Nach Spec stehen Islamicly und Musaffa gleich bei 5,0, Islamicly gewinnt per Name. Das bricht den Test `vergleichAssistent.test.ts:391-397`.
15. **`offeneAnfragen`.** Schlüssel ist das Haus (`anfragenLog.ts:8`). Steuer, Screener und Edelmetall haben kein `haus`. Prime+ hat `haus: "scalable-prime"`, das Log führt „scalable“ (`anfragenLog.ts:202`). Automatische Antworten stehen nur im Text. Ein ausgehender Vorgang liegt in der Zukunft (28.09.2026).
16. **Anzeige.** Die Spec will zwei Nachkommastellen (Z. 30), `noteZahl` nutzt `toFixed(1)` (`vergleichTypen.ts:88-89`). Die Spec will halbe Sterne (Z. 31), `Sterne` füllt stufenlos (`VergleichsBausteine.tsx:25-28`). `abgeraten` bedeutet laut Typ „Zins-Merkmal rot“ (`vergleichTypen.ts:66-71`).
17. **Test 2.** `partnerLinks.aktiv` hängt nicht am Anbieter, sondern steht in `partnerLinks.ts:21`. Dafür fehlen Felder, die tatsächlich am Anbieter hängen: `finanzfluss.rang`, `finanzfluss.partnerlink` (`vergleichHelfer.ts:51`), `etikett`, `note` (Z. 37-39).
18. **Letzter Gleichstand.** Z. 38 vergleicht zweistufig, `werteAus` vergleicht einen verketteten String (`vergleichAssistent.ts:331`). Ein letzter Brecher über die `id` fehlt.

## LÜCKEN

- Pflichtfelder je Kategorie sind nicht aufgezählt. Vorschlag Depot: Tor, 3 exakte Zeilen mit N = 12/3/7, `halalAnlagenPunkte` je Zeile als Zahl und nicht leere `finanzPunkte`.
- `finanzPunkte: {}`: SumUp hat leere Punkte (`girokontoNachtraege.ts:31`). Das gilt in `bewertung.ts:153` als vorhanden und ergibt Kosten 0. Es muss als fehlend zählen.
- Vorrang der Gruppen ist offen. Vorschlag: abgeraten vor nichtBewertet vor gerankt (Interactive Brokers: Tor rot mit Lücken; Steuerbot: „kapital nein“ mit Lücken).
- Edelmetall: Bei „Zertifikate und CFDs“ sind beide Tore rot (`edelmetallVergleich.ts:243-244`), welcher Grund gilt, ist offen. `kosten = 0` würde als „Kosten 0,0“ angezeigt, besser `null`.
- Steuer: `plattform: null` würde als „läuft überall“ zählen. Ein Preis ohne Eurobetrag ergibt in `euro()` null (`vergleichAssistent.ts:132-136`), „1.234,56 €“ wird falsch gelesen. Die Werte „kapital: ja, …“ und „nur in der Fassung …“ sind ungeregelt.
- Screener: „unbekannt“ und `kostenlos: null` sind ungeregelt. `standard` und `deutscheAktien` sind Halal-Zeilen ohne Formel.
- Sortierung: offen, ob auf gerundeten oder ungerundeten Werten sortiert wird (Empfehlung: gerundet). Für nichtBewertet und abgeraten fehlt eine Reihenfolge (heute `vergleichAssistent.ts:334`).
- `halal` bei Tor „teils“: offen, ob vor oder nach der Halbierung ausgegeben (`bewertung.ts:161,166` gibt halbiert aus).
- Angezeigte Note bei Priorität: Vorschlag immer `rangfolge.note`, die Priorität wirkt nur auf die Sortierung.
- `werteAus.ungeprueft` und nichtBewertet: Verhältnis nicht definiert.
- Benennung: `halal` trägt bei Steuer die Leistung, beim Screener die Transparenz. Beschriftungen je Kategorie mitliefern.
- `anfragenOffen`-Skript: Datum als Text, Vorgänge in der Zukunft, automatische Antworten und die Frische der Datei sind ungeregelt.

## TESTS (fehlen in §7)

- Kosten: `finanzPunkte` fehlt oder `{}` ergibt nichtBewertet; negativer Abzug, negativer Einzelwert (bunq Elite kontofuehrung −1), Begrenzung auf 0 bis 5.
- Depot: exakte Zeile ohne `halalAnlagenPunkte`-Schlüssel ergibt fehlt (kein Rückfall auf x); N ungleich 12/3/7 ergibt fehlt; Tor null oder „unbekannt“ ergibt fehlt `["zinsfreiAbStart"]`; Datentest: jede exakte Zeile hat Punkte.
- Giro: Dispo „teils“ zählt 0,5 (Haspa). Dispo „schlecht“ bleibt gerankt (DKB).
- Krypto: zinsfreiesModell „schlecht“ und echteCoins „schlecht“ bleiben gerankt.
- Steuer: `belegabruf` true, false und null; die Werte „ja, …“ und „nur in der Fassung …“; `plattform: null`, Preis ohne Euro, Preis über 60 ergibt 0, Abo gegen einmalig.
- Screener: Boolean-Felder, Islamicly wird gleich wie `kostenlosReicht` gewertet, Finispia landet in nichtBewertet, Musaffa bleibt Platz 1.
- Edelmetall: Grund bei zwei roten Toren, `ausliefern: teils`, Reihenfolge bei Kosten-Gleichstand.
- Rundung und Sterne: höchstens 2 Nachkommastellen im Bereich 0 bis 5; halbe Sterne an den Grenzen 4,24, 4,25, 4,74, 4,75.
- Gleichstand: Brecher über Kosten, Halal, Name, Produkt, `id`; geteilter Platz, falls gewählt.
- `offeneAnfragen`: wirkt nur auf nichtBewertet; Rückfall ohne `haus`; Prime+.
- Generator für `anfragenOffen`: Zukunft und automatische Antworten zählen nicht; keine Adressen oder Tickets in der Ausgabe; Frische gegen `anfragenLog.ts`; `anfragenLog` wird von keinem ausgelieferten Modul importiert.
- Reinheit: Eingabe mit `Object.freeze` einfrieren; `rangfolge.ts` importiert weder `partnerLinks` noch `deals`.
- Test 2 erweitern auf `finanzfluss.rang`, `finanzfluss.partnerlink`, `etikett`, `note` und `noteStand`.
- `werteAus`: ohne Antworten dieselbe Reihenfolge wie `gerankt`, nicht nur dieselbe Nummer 1; eine Priorität ändert nie `halal` oder die Gruppe; `ungeprueft` enthält nichtBewertet; `empfehlbar` verhält sich unverändert.
- Echte Daten: jede Kategorie, jeder Anbieter in genau einer Gruppe.
- Umschreiben über `vergleichAssistent.test.ts:70` hinaus: `vergleichAssistent.test.ts:3,25-27,32,40,47,56-65,83,88-104,106-118,188-191,216-221,364-369,391-397`; `bewertung.test.ts:92-99` und die „8 von 8“-Fälle; `vergleiche.test.ts:9,57-60,95-105`; `rangfolgeIntern.test.ts` (nutzt `bewerte`); Python `test_anlagen_regeln.py:89-94,134-138`.

## OFFENE FRAGEN (Empfehlungen des Prüfers)

1. Unklarer Aufschlag in die Pipeline, genauer `anlagen_regeln.py:185-191`: nur dort ist der einzelne Fonds bekannt. Faktor „unklar“ und fehlende Angabe = 0,5, dazu die Zahl der unklaren Fonds mitschreiben. `rangfolge.ts` wertet null oder einen fehlenden Schlüssel defensiv als fehlt.
2. Steuer „kapital = nein“: abgeraten lassen, mit neutralem Grund. Ein geprüftes Nein als „noch nicht geprüft“ wäre falsch. Typ-Kommentar zu `abgeraten` (`vergleichTypen.ts:66-71`) auf „ungeeignet, mit Grund“ verallgemeinern, Anzeige in P4 ohne Zins-Rot.
3. Preisformel linear bis 60 €. Stufen legen 34,90 bis 39,99 € zusammen, dann entscheidet das Alphabet, und haben Kanten (19,99 gegen 20,01 €). Welcher Betrag gilt (einmalig vor Abo), gehört als Regel oder Datenfeld dazu.
4. Gleichstand: gleicher Platz, wenn Note, Kosten und Halal nach Rundung gleich sind. Innerhalb bleibt die Reihenfolge nach Name und `id` fest, `platz` zählt 1, 1, 3. Der Kasten „Nummer 1“ nur bei eindeutigem Platz 1, sonst „gleichauf“ (P4). Test 4 vergleicht dann die Menge auf Platz 1.

## PARTNER/FINANZFLUSS

- Finanzfluss-Rang: `lib/vergleichAssistent.ts:278-280` baut die Rangnote, `:300` nutzt sie, über `sortWert` (`:318`) und die Ordnung (`:326-331`) landet sie in `passt[0]`. Das ist heute die „Nummer 1“ der Seite (`VergleichsSeite.tsx:150-162`, Etikett `:164-171`). Quelle der Zahl: `bauen.py:580` (`rang: i + 1`).
- Islamisches Paket: `lib/vergleichAssistent.ts:194-216`, eingehängt in `:327`.
- Eingabereihenfolge: `lib/vergleichAssistent.ts:282,330` für kategorie null (Steuer: handsortierte Preisliste); `vergleichKorrekturen.ts:44-47` und `bauen.py:601`: alphabetisch, Abgeratene am Ende. `rangfolge` darf davon nicht abhängen.
- Zusatzsortierung: `nebenSort` und `grundSort` (`data/vergleichAssistent.ts:246-250,338-350,571-575,647-653`), halalAnteil 0,65 (`:217`), `RANGFOLGE_FREI` in `lib/vergleichAssistent.ts:31-36,263,302,322`.
- Partnerlinks: fließen in `werteAus` nicht ein. `bauen.py:578,600` setzt `link` und entfernt ihn bei Abgeratenen, ohne Wirkung auf die Reihenfolge. Partner vorn stehen in `src/lib/anbieterLogos.ts:21` und `src/components/anlage/VerfuegbarBei.tsx:11-14` (Logo-Reihe, P4).
- `aktiv` (`partnerLinks.ts:21`) wird von keiner Rangfolge gelesen. Deals: `deals.ts:127,145` sortiert nur Boni, `pages/VergleichAssistent.tsx:79-86` zeigt den Bonus ohne Wirkung auf die Reihenfolge.

## UMSETZUNGSREIHENFOLGE (TDD)

1. Entscheidungen festschreiben (Befund 1 und 2, vier offene Fragen).
2. Pipeline (Python): `test_anlagen_regeln.py` rot, dann `anlagen_regeln.py`, Docstring in `anlagen_verdichten.py` korrigieren, `anlagen_verdichten.py` ausführen, `bauen.py` auf `rangfolge` (erzeugt `brokerVergleich.ts` neu).
3. `src/lib/rangfolge.test.ts` neu, rot: Einheitentests je Formel, Gruppen, Unabhängigkeit, Reinheit.
4. `src/lib/rangfolge.ts` neu: Typ mit 6 Werten, Konstanten, `opt.finanzMax` mit Standard je Kategorie. Leser `anteil`, `euro`, `finanzNote` in ein eigenes Modul, damit kein Zirkel zu `vergleichAssistent` entsteht. `HALAL_REGELN` und `FAKTOR_ABSCHALTBAR` aus `bewertung.ts` übernehmen.
5. Generator und erzeugte Datei: Generator in `scripts/`, `src/data/anfragenOffen.ts`, Frischetest. `haus` für Prime+ und Rückfall auf die `id` klären.
6. `vergleichAssistent.test.ts` umschreiben (Liste oben), Test für dieselbe Nummer 1 ergänzen.
7. `src/lib/vergleichAssistent.ts`: `werteAus` auf `rangfolge` aufsetzen. Entfernen: `RANGFOLGE_FREI`, Rangnote, islamisches Paket, `halalBelegt`, Parameter `frei`, Pfad für null. Umgang mit `BASIS` und „teils“ entscheiden.
8. `src/data/vergleichAssistent.ts`: `kategorie` „screener“ und „steuer“ setzen, halalAnteil entfernen, `nebenSort` und `grundSort` streichen oder auf Nicht-Halal beschränken, Satz in Z. 611 anpassen, `empfehlbar` prüfen.
9. Altlasten: `bewertung.ts` und `bewertung.test.ts` auslaufen lassen; `vergleiche.test.ts`, `rangfolgeIntern.test.ts`, `scripts/pruefstand.ts:63` auf `rangfolge` umstellen; Doku in `vergleichHelfer.ts:55-59`.
10. Zuletzt, eher P4: Nummer 1 in `VergleichsSeite.tsx:150-162` aus `rangfolge` (gemeinsame Funktion `nummerEins`), `noteZahl` und Sterne anpassen.
